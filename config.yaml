jobs:
- name: test-git
  plan:
  - task: add-one
    config:
      platform: linux
      image_resource:
        type: mock
        source: {mirror_self: true}
      run:
        path: sh
        args:
        - -c
        - |
          echo "niteesh from script"
